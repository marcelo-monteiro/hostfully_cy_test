{
  "scripts": {
    "open": "cypress open",
    "tests:report": "cypress run --env allure=true --browser chrome --config video=true"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^13.1.0",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "cypress": "^11.0.0",
    "yamljs": "^0.3.0"
  }
}
